export enum USER_ROLES {
  TEACHER = 'teacher',
  STUDENT = 'student',
}

export enum FILE_TYPES {
  VIDEO = 'video',
  IMAGE = 'image',
}

export const PROTECTED_ROUTES = ['/dashboard', '/manage-course', '/my-courses', '/course-details'];
